---  
// interface Props { 
//     description: string;
// } 
// const { description } = Astro.props;
---
<!-- 
<script>
	import { gsap } from "gsap";

	import { ScrollTrigger } from "gsap/ScrollTrigger"; 

	gsap.registerPlugin(ScrollTrigger); 

	const tl = gsap.timeline();   
	tl.fromTo(
			'.child',
			{
				xPercent: -100,
			},
			{
				xPercent: 10,
				ease: "Power4.easeOut",
				scrollTrigger: {
					trigger: '.parent',
					start: "-50% top",
					end: "=+50%",
					scrub: true, // Smooth scrolling animation
					toggleActions: "play none none reverse",
					markers: false,
				},
			},
		); 
</script> 

<section class="flex flex-col md:gap-24 items-center md:py-32  md:max-w-screen-xl mx-auto">
    <div class="w-full flex items-center relative h-halfscreen"> 
        <div class="flex w-full justify-between"> 
            <p class="text-5xl md:text-6xl" set:html={description}></p>
        </div>
    </div> 
</section>


<style> 
	.linear-gradient{
		background:  linear-gradient(270deg, #ff38bb 4.25%, #ff8038 51.61%, #f5be66 80.43%, #f4dca8 93.03%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}
</style> -->