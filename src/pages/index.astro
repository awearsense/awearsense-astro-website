---
import Layout from "../layouts/Layout.astro";

const response = await fetch("https://jsonplaceholder.typicode.com/posts");
const data = await response.json();

const images = [
	"logo-nsrcel.png",
	"logo-iit-hyderabad.png",
	"logo-nsrcel.png",
	"logo-iit-hyderabad.png",
	"logo-nsrcel.png",
	"logo-iit-hyderabad.png",
];
---

<script>
	import { gsap } from "gsap";

	import { ScrollTrigger } from "gsap/ScrollTrigger";
	import { Observer } from "gsap/Observer";
	import { ScrollToPlugin } from "gsap/ScrollToPlugin";
	import { MotionPathPlugin } from "gsap/MotionPathPlugin";

	gsap.registerPlugin(ScrollTrigger);

	const tl = gsap.timeline();
	const msg = [
		{ parent: ".b", child: ".focus", startPos: "-50%", endPos: "0" },
		{ parent: ".c", child: ".relax", startPos: "100%", endPos: "0" },
		{ parent: ".d", child: ".control", startPos: "-50%", endPos: "0" },
	];
	msg.forEach((elm) => {
		console.log(elm);
		tl.fromTo(
			elm.child,
			{
				x: elm.startPos,
			},
			{
				x: elm.endPos,
				ease: "Power4.easeOut",
				scrollTrigger: {
					trigger: elm.parent,
					start: "-50% top",
					end: "=+50%",
					scrub: true, // Smooth scrolling animation
					toggleActions: "play none none reverse",
					markers: false,
				},
			},
		);
	});


	tl.to(".f-video",{
				width:"100%", 
				height:"70%", 
				borderRadius:'1.5rem',
				scrollTrigger: {
					trigger: ".f-width",
					start: "center center",
					end: "=+50%",
					pin:true,
					scrub: true, // Smooth scrolling animation
					toggleActions: "play none none reverse",
					markers: false,
				},
			},
		); 
</script>

<Layout title="Welcome to Astro.">
	<main class="container md:max-w-screen-xl mx-auto">
		<section
			id="main"
			class="h-screen flex justify-center items-center relative"
		>
			<div class="flex flex-col text-center">
				<span
					class="text-10xl font-normal tracking-tighter text-white drop-shadow-sm"
					>awearsense</span
				>
				<span class="tracking-wide font-extralight text-white">
					Meet our first generation of
					<span class="font-medium">neural interface</span>. Control
					digital devices with your
					<span class="font-medium">brain</span>
				</span>
			</div>
		</section>

		<section class="b h-screen flex items-center">
			<div class="w-full relative">
				<h1 class="focus text-largest font-bold tracking-wide text-gray-300">
					Focus
				</h1>
				<p class="text-6xl text-gray-700 absolute">
					Achive Your Peak Performance
				</p>
			</div>
		</section>
		<section class="c h-screen flex items-center">
			<div class="w-full relative">
				<h1 class="relax text-largest font-bold tracking-wide text-gray-300">
					Relax
				</h1>
				<p class="text-6xl text-gray-700 absolute">Gain Mental Clarity</p>
			</div>
		</section>
		<section class="d h-screen flex items-center">
			<div class="w-full relative">
				<h1 class="control text-largest font-bold tracking-wide text-gray-300">
					Control
				</h1>
				<p class="text-6xl text-gray-700 absolute">
					Think & Control Your Device
				</p>
			</div>
		</section>

		<section class="h-screen relative f-width ">
			 <div class="absolute bg-green-400  -translate-x-1/2 left-1/2 -translate-y-1/2 top-1/2 w-screen h-full f-video">
				 
			</div>
		</section>
		<section class="h-screen flex items-center">
			<div class="grid grid-cols-2 w-full list-items">
				<div>
					<h2 class="text-6xl">Section Title 1</h2>
					<p>
						Lorem ipsum dolor sit amet consectetur adipisicing elit.
						Totam temporibus, possimus aut numquam iste debitis
						consectetur nihil quos dolorum, animi iusto expedita
						ullam veniam cupiditate, odio enim quibusdam non ex.
					</p>
				</div> 
			</div>
		</section>
		<section id="partners" class="h-halfscreen flex items-center">
			<div class="flex w-full gap-16">
				<div class="flex flex-col gap-4">
					<h1 class="text-6xl font-semibold tracking-wide">
						trusted partners
					</h1>
					<div class="mb-6">
						<input
							type="text"
							id="large-input"
							class="block w-full p-4 text-gray-900 border border-gray-300 rounded-lg bg-gray-50 sm:text-md focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500"
						/>
					</div>
				</div>
				<div
					class="flex gap-4 justify-between items-center overflow-hidden"
				>
					{
						images.map((item) => (
							<img
								src={"/src/assets/partners/" + item}
								alt={item}
								class="object-contain h-16"
							/>
						))
					}
				</div>
			</div>
		</section>
		<section id="technical-details" class="h-screen">
			<h2 class="text-4xl font-semibold tracking-wide py-6">
				technical specifications
			</h2>
			<div class="flex flex-col border rounded-md">
				<div class="specification-row">
					<span>EEG Channel</span>
					<span>4 + (2 reference)</span>
				</div>
				<div class="specification-row">
					<span>Electrode Type</span>
					<span>Soft Dry</span>
				</div>
				<div class="specification-row">
					<span>Channel Location</span>
					<span>In-ear & Around Ear</span>
				</div>
				<div class="specification-row">
					<span>Resolution</span>
					<span>16-bit</span>
				</div>
				<div class="specification-row">
					<span>Sampling Rate</span>
					<span>250Hz</span>
				</div>
				<div class="specification-row">
					<span>Sensor</span>
					<span>3-axis IMU</span>
				</div>
				<div class="specification-row">
					<span>Connectivity</span>
					<span>Blutooth Low Energy</span>
				</div>
				<div class="specification-row">
					<span>Battery Life</span>
					<span>6 hrs</span>
				</div>
				<div class="specification-row">
					<span>Audio</span>
					<span>Stereo Class-D Headphone Amplifier with 98db SNR</span
					>
				</div>
				<div class="specification-row">
					<span>Dimensions</span>
					<span>8" x 6.5" x 3"</span>
				</div>
				<div class="specification-row">
					<span>Weight</span>
					<span>15.61g</span>
				</div>
				<div class="specification-row border-none">
					<span>Supported Platform</span>
					<span class="table-row-item flex flex-col">
						<span>Windows 10 (64 Bit)</span>
						<span>Android 5.0 and above</span>
					</span>
				</div>
			</div>
		</section>
	</main>
</Layout>

<style>
	#main::after {
		content: "";
		z-index: -1;
		position: absolute;
		left: 50%;
		transform: translate(-50%);
		width: 100vw;
		height: 100%;
		background-image: url("../assets/bg.jpg");
		background-repeat: no-repeat no-repeat;
		background-position: center;
		background-size: cover;
	}

	.specification-row {
		@apply grid grid-cols-2 border-b border-slate-100 p-4  text-slate-500 dark:text-slate-400;
	}

	/* .table-row-item:nth-child(1){
		@apply text-slate-600 font-semibold;
	} */

	.specification-row:nth-child(odd) {
		@apply bg-slate-50;
	}
</style>
